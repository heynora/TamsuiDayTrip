<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nora.tamsui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SingInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Random" />
        <activity android:name=".Introduction" />
        <activity android:name=".Tamsui_menu"></activity>
        <activity android:name=".CreateData_Activity"></activity>
        <activity android:name=".Notified_Data_Activity"></activity>
        <activity android:name=".Download_Activity"></activity>
    </application>

</manifest>